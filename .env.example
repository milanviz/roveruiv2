NEXT_PUBLIC_API_URL=
# Platform realtime socket server (the legacy socket.io server), used by the
# Realtime Push block. Its own host and port — not the API origin.
NEXT_PUBLIC_SOCKET_URL=https://chat.qa.hirover.ai

# Tenant id for the tenant-wide socket room. Optional: a Realtime Push
# addressed to a user id arrives without it.
NEXT_PUBLIC_TENANT_ID=11
